import { SortingHatUser } from './sorting-hat-user.entity';

describe('SortingHatUserEntity', () => {
    it('should be defined', () => {
        expect(new SortingHatUser()).toBeDefined();
    });
});
